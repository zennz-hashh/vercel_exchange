### Supabase configuration (paste into .env and fill values)
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-public-key

# If you have a service_role key (server-side only), keep it secret and do NOT expose it to the frontend
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
